/**
 * configuration template for ErstiWeTool
 */

module.exports = {
  http_port: 3333,

  // affects the database queries & email templates
  year: new Date().getYear() + 1900,

  // if you change these values, also change them in schema.sql !
  mysql: {
    database: 'ersti-mails',
    host: 'localhost',
    user: 'ersti-mails',
    password: 'test',
    connectionlimit: 25,
  },
};

